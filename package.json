{
  "name": "drought-bulletin-webcrawler",
  "version": "1.0.0",
  "description": "Experimental AI Drought Bulletins - Full Stack Application",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && source venv/bin/activate && python main.py",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run preview",
    "clean": "pkill -f 'vite' && pkill -f 'python.*main.py' && pkill -f 'uvicorn'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["drought", "ai", "webcrawler", "fastapi", "react"],
  "author": "",
  "license": "MIT"
}
